---
export interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<section class={`py-12 px-4 ${className}`.trim()} id={id}>
  <div class="max-w-5xl mx-auto">
    <slot />
  </div>
</section>
